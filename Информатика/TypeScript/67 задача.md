```typescript
function addBinary(a: string, b: string): string {
    let bigA = BigInt("0b" + a)
    let bigB = BigInt("0b" + b)
    let bigAB = bigA + bigB
    let answer: string = bigAB.toString(2)
    return answer
};
```

![[Pasted image 20260120100258.png]]