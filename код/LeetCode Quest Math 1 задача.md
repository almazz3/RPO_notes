```python

```

Сначала сортируем список `arr` (чтобы числа шли по порядку от меньшего к большему). Дальше вычисляем разницу между числами (2 и 1 элементом списка). Если список состоит из 2х элементов, выводится `True`. Идёт перебор по длине списка. Если 